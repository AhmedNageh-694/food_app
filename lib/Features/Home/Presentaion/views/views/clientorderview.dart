import 'package:flutter/material.dart';

class ClientOrderView extends StatelessWidget {
  const ClientOrderView({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView.builder(
      itemBuilder: (context, index) {
        return ;
      },
    );
  }
}
